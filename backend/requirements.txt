# ── QUERYSENTINEL Backend Dependencies ──────────────────────────────────────
# Pin exact versions for reproducible builds

# MongoDB
pymongo[srv]==4.9.2          # Change Streams, Atlas operations (bundles bson)

# Web framework (ranges so pip resolves cleanly with google-adk's deps)
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
sse-starlette>=2.2.1         # Server-Sent Events for live dashboard
pydantic>=2.11.1
python-multipart>=0.0.20

# Google AI / ADK
google-adk==1.3.0            # Multi-agent orchestration + MCP toolset
google-genai>=1.17.0         # Gemini 3.5 Flash underlying SDK (ADK 1.3.0 requires >=1.17.0)

# MCP SDK — QuerySentinel exposes its own anomaly tools as an MCP server
mcp>=1.9.0                   # Official MCP Python SDK (FastMCP + SSE transport)

# Data & statistics
numpy==2.1.3
scipy==1.14.1                # stats.zscore, normal distribution
pandas==2.2.3                # DataFrame for semantic velocity aggregation

# Utilities
python-dotenv==1.0.1
faker==33.0.0                # seed script — generate realistic anomaly descriptions
httpx>=0.28.1                # async HTTP for health checks

# NOTE: fastembed (local embeddings) is installed in the local/dev environment
# for full vector search. It is intentionally NOT in this hosted requirements
# file — onnxruntime + google-adk together exceed the 512MB free-tier limit.
# The detection core degrades gracefully without it (vector-search badge only).

# Tamper-evident cryptographic audit receipts (Ed25519) for incident reports
aetherproof>=0.2.0
